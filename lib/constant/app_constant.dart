import 'package:flutter/material.dart';
//we can start over constant we k , after that we can easily find const by k.

const kActivecolor = Color(0xFF52F2BF);
const kInactivecolor = Colors.teal; // we can use this color by this name.
const kBottomContainerHeight = 80.0;
const kBottomContainerColour = Color(0xFFEB1555);
const kParameternumberStyle =
    TextStyle(fontSize: 50, fontWeight: FontWeight.w900);
const kTextstyle = TextStyle(fontSize: 30, fontWeight: FontWeight.bold);
const kParameterstyle = TextStyle(fontSize: 30, fontWeight: FontWeight.bold);
const kLargeButtonTextStyle =
    TextStyle(fontSize: 25.0, fontWeight: FontWeight.bold, color: Colors.black);
